import Token, Tokeniser from ./tokeniser
import ParsedType, TypeKind, Statement, StatementKind from ./parser

function parser(tokeniser: Tokeniser)
    var tk: ParsedType[string]
    var items: int[]

    const stmt := Statement(kind: StatementKind.LetStatement)
    const t := ParsedType(kind: TypeKind.boolType, stmt)

    const lessThan10 := items.filter(where: it < 10)

end
